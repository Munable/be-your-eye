{
  "name": "@be-your-eyes/supabase-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test *.test.ts"
  },
  "dependencies": {
    "@be-your-eyes/catalog-validator": "file:../../model-tools/catalog-validator"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
